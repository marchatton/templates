# Template Releases

## v0.2.0 - 2026-02-18
- Added profile-based scaffold settings (`minimal` and `full`).
- Added per-surface controls (`docs`, `skills`, `agents`) and skills packs (`core`, `extended`).
- Added existing-repo safe flow with dry-run plan mode and explicit `--apply`.
- Added generated docs structure output and starter docs templates.
- Added smoke-test script and CI updates for template validation.

## v0.1.0 - 2026-02-06
- Initial scaffold scripts, docs structure, and AGENTS template set.
